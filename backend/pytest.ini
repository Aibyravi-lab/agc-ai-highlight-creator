[pytest]
# Scope discovery to tests/ only. backend/ also contains ad hoc, manual
# debug scripts (test_audio.py, test_caption.py, test_whisper.py,
# test_whisper_original.py) that execute side-effecting code at import
# time and are not meant to be collected by pytest.
testpaths = tests
